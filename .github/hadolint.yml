failure-threshold: warning

ignored:
  # ignore using latest tag instead of sha digest
  - DL3007

override:
  warning:
    # encourage pin apt package versions as a warning, debian package updates preferred
    - DL3008
    # encourage --no-install-recommends
    - DL3015
